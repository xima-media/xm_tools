# cat=Backend; type=boolean; label=Enable backend colorization according to application context
backendMarkingEnabled = 1

# cat=Backend; type=boolean; label=Show the version property from the project's composer.json if available
showWebsiteVersionInBackend = 1
